{% extends 'base.html.twig' %}
{% block body %}
    <ul>
        {% for category in categories %}
            <li><a href="{{ path("category_products", {'id':category.id})}}">{{ category.name }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}


